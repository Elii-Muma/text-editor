{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "text-ctrl",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/build/text-ctrl.exe",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "environment": [],
            "externalConsole": true, // 👈 this is what you want
            "MIMode": "gdb",         // or "lldb" or "gdb" or "dbg" depending on your setup
            "miDebuggerPath": "gdb", // optional
            "setupCommands": [
                {
                    "description": "Enable pretty-printing",
                    "text": "-enable-pretty-printing",
                    "ignoreFailures": true
                }
            ]
        }
    ]
}
